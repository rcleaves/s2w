<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cardview_light_background" >


    <ImageView
        android:id="@+id/contest_photo"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:padding="2dp"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/contest_text"
        android:layout_width="260dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="6dp"
        android:layout_marginLeft="6dp"
        android:layout_marginRight="6dp"
        android:layout_marginTop="6dp"
        android:layout_toRightOf="@+id/contest_photo"
        android:ellipsize="end"
        android:singleLine="false"
        android:text="caption"
        android:textColor="@android:color/primary_text_light"
        android:textSize="20dp" />

    <ImageView
        android:id="@+id/contest_like_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="right"
        android:layout_toRightOf="@+id/contest_text"
        android:padding="4dp"
        android:src="@drawable/heart_full" />

    <TextView
        android:id="@+id/contest_num_likes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="--"
        android:layout_toRightOf="@+id/contest_like_icon"
        android:textColor="@android:color/secondary_text_light"
        android:gravity="right"
        android:textSize="24sp" />

</RelativeLayout>
